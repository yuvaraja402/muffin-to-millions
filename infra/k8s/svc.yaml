apiVersion: v1
kind: Service
metadata:
  name: my-microservice
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
spec:
  type: LoadBalancer
  selector:
    app: my-microservice
  ports:
    - port: 80
      targetPort: 5050
      protocol: TCP